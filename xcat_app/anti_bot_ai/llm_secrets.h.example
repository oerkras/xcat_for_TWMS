#pragma once

// Copy to llm_secrets.h and fill in real keys (llm_secrets.h is gitignored).
// Used by anti-bot LLM fallbacks when ini does not override.
namespace xcat::app::anti_bot_ai {

inline constexpr const char* kEmbeddedQwenApiKey = "";

inline constexpr const char* kEmbeddedDeepSeekApiKey = "";

}  // namespace xcat::app::anti_bot_ai
