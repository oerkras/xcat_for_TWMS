#pragma once

namespace xcat::app::net_path {

// 由 UI 主循环每帧调用；内部自节流（约 8s 一次），不起线程。
//
// 用途：断线取证时区分「直连台服」与「走加速器隧道」。2026-08-13 那次多机同秒断线
// 排查了一整轮客户端侧才定位到共用加速器，就是因为 kick.log 看不出流量走哪条路。
//
// 对游戏进程零接触：只向内核查系统 TCP 表与本机网卡，不开游戏进程句柄、不读其内存、
// 不注入、不往游戏进程加载任何模块——因此不新增任何检测面（详见
// docs/features/security/检测面盘点与187秒墙.md 的口径）。
//
// 口径：只看 IPv4 已建立连接（经典版实测走 IPv4）；多开时每个实例各记一行，行首带 pid。
// 仅在路径变化时记录，另每 120s 落一次心跳锚点。
void Tick();

}  // namespace xcat::app::net_path
