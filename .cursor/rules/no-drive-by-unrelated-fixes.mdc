---
description: 构建/编译遇无关错误时不擅自修复，只完成本职改动
alwaysApply: true
---

# 无关错误不擅修（本职优先）

构建、链接、类型检查若报错，且**错误不在本轮任务改动的文件/模块内**（旁路 feature、他人未完成改动、既有技术债）：

## 必须

- 只完成本职改动；验证时注明：本职相关目标已编译 / 全量构建被无关错误阻断（路径 + 错误摘要）。
- 把无关失败标为范围外，交还用户决定是否另开任务修。

## 禁止

- 为「凑绿」顺手改无关文件（例如修 `pet_loot` / `invuln` 只为让 `auto_lie` 全量过编）。
- 扩大 diff、顺带重构、补未授权的 API/日志「小修」。

## 例外（仅当用户当轮明确要求）

- 用户说「顺便修编译 / 把全仓编绿 / 修那个报错」才可动无关文件。
- 本职改动**直接引入**的错误必须修。

## 示例

| 场景 | 做法 |
|---|---|
| 改 `auto_lie`，全量构建挂在 `pet_loot` 缺符号 | 不改 `pet_loot`；报告阻断点；能单编本职目标则单编验证 |
| 本轮改坏了 `anti_macro_port.cpp` | 必须修 |
| 用户：「先把编译错误都清掉」 | 可修无关错误 |
