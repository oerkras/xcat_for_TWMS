---
description: 经典版客户端更新后 GameAssembly remount（换 dump.cs 哈希/方法头 RVA）。触发词：remount、ga_remount、客户端更新、换偏移、dump.cs。
globs: scripts/ga_remount.py,scripts/data/ga_patch_sites.tsv,scripts/data/ga_remount_ignore.txt,scripts/data/ga_krva_bind.tsv,scripts/data/ga_remount_smoke_expect.tsv
alwaysApply: false
---

# GA remount（Agent 硬停）

产品 = 经典版 / TWMS。接到本任务先读 [`docs/features/ops/GA-remount-Agent清单.md`](../../docs/features/ops/GA-remount-Agent清单.md)，或跑 `python scripts/ga_remount.py howto`。

## 必须

- 只换 dump 能证明的**类/字段/方法哈希**和**方法头 RVA**。
- 体内点只核 `scripts/data/ga_patch_sites.tsv`；编码变了先跑 `catalog`（父方法窗口唯一命中），再 IDA。**禁止**在平坦化里盲搜 `75 07`。
- `catalog --write` 仅当用户当轮说 write/apply；改 tsv 的 rva/target **和** 该行 `cpp` 列的 `constexpr kRva*`（`kRvaMagicCmov` 等）。禁止改注释里的裸 `0xHEX`。
- `map` 默认 dry-run。`--apply` / `--apply-hashes` / `--apply-krva` 仅当输出里 `on_old_map>0` **且用户当轮明确说 apply**。
- `--apply` = 哈希 + 具名 `kRva*`。**禁止** `--apply-all-hex` 除非用户点名。
- 哈希已在新 dump 上时 `--apply-all-hex` 会 REFUSE；`--apply-hashes` 会 skip。
- apply 哈希之后跑 `layout`，再 `--apply-krva`，再 `krva` / `audit`。
- `layout` 的 `TYPE_FLIP` / `FALLBACK_STALE` 必须改 `kFb*`。`layout --write` 仅当用户当轮说 write，且只改 `FALLBACK_STALE` 的 `constexpr kFb*`。
- `krva MATCH` 含同一哈希的 override 方法头；`--apply-krva` 走 `ga_krva_bind.tsv`（hash/plain；`shared=1` 禁止唯一改写）。`BIND_STALE` 时用户说 write 才 `--write-bind`。
- 注入后 `smoke` 对照 `ga_remount_smoke_expect.tsv`（`need=req` 缺组才红；必过组可回退更早 bind 会话），并扫日志轮转。
- `dump --process` 仅当用户当轮说 process/ForceDump；禁止与 `--archive` 同时；禁止藏进自定义注入。
- `verify` 只干跑（含 map/layout），不 apply、不 ForceDump。
- `apply --apply` 仅当用户当轮说 apply/write。顺序 hashes → kFb → kRva → catalog；`TYPE_FLIP` / catalog FAIL 立刻停，不再写后面的步。

## 禁止

- 改业务判定、补丁策略、if/else、默认开关。
- 把源码里任意 `0xHEX` 当方法头替换（注释/种子/体内点/数字重用会改坏活函数）。
- 杀用户进程、打包发布、清浏览器登录态。
- 磁盘壳 `GameAssembly.dll`（~36MB）当 IDA 真源；必须用 `Dumps/runtime/` 运行时展开 dump。
---
