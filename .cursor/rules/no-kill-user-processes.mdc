---
description: 禁止代用户强杀已运行进程；链接占用时由用户自行关闭
alwaysApply: true
---

# 进程与产物占用

- **禁止**用 `taskkill` / `Stop-Process` / 结束任务等方式关闭用户正在运行的进程（含 `xcat.exe`、`MscLauncher.exe`、游戏客户端等）。
- 构建若报 `LNK1104` 等「无法打开文件 / 被占用」：只说明原因与要关的进程名，**等用户自行关闭**后再重试链接；不要主动杀进程。
- 仅当用户**明确要求**关闭某个进程时，才可执行结束命令。
- 调试、热重载、冒烟测试同样遵守：不擅自结束用户已开实例。
