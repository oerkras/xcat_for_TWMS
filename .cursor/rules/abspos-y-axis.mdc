---
description: 经典版 AbsPos/Y——更大 Y=更高。禁止因符号相反、PetLoot、传统 MS WY 而给瞄准 Y 反号
alwaysApply: true
---

# AbsPos 坐标系（经典版 / TWMS）

位移 / 贴门 / 旋翼 / 贴 NPC 的真源是 **`VecCtrl.AbsPos`（Ap）**（`ReadLocalAp` / `QueryFlightState` / LocalUser 与 NPC 的 ActorPos 同字段）。

## 铁律（已用实机打穿）

| AbsPos | 含义 |
|--------|------|
| **Y 增大** | **更高**（远离地面 / 远离掉落方向） |
| **Y 减小** | **更低**（掉落方向） |

- 抬高 = **`aimY += lift`**。禁止 `aimY -= lift` 当抬高（会压到台下无限掉）。
- **两个 AbsPos 符号相反 = 高度差，不是两套坐标。** 港面 `ap.y=-287`、桑克斯 ActorPos `y=219`：他站得更高。禁止写成 `hintY = -npc.y`。

### 证据

- 2026-08-08 贴门：`aimY-=24` → 人掉到台下 `ap.y=-735`。抬高必须 `+=`（`kPortalAimLiftY`）。
- 2026-08-14 贴桑克斯：把 `y=219` 反号成 `-219` → `aimFh=0`、人在空中 `y≈-210` 晃、`NOT_STOOD`。PetLoot 写 `pos=(-59,287)` 时人在 `(-59,-287)`——那是显示翻转，**禁止拿来给 Ap 瞄准反号**。

## 口径（不得混用）

| 口径 | 用法 |
|------|------|
| **AbsPos / ActorPos（同字段）** | 瞄准、贴门、贴 NPC、旋翼 setpoint。NPC 与人 dy 大 = 真高差。 |
| PetLoot / 部分 UI `worldY=-Ap.Y` | 只读显示。改瞄准前对照 `QueryFlightState` 的 `st.y`。 |
| 传统 MS WY / 注释「更小 Y=上」 | 不要套到 AbsPos 加减。 |
| foothold 图论 ClimbUp「朝更小 Y」 | 改 Ap 瞄准时以上表为准；改边语义前先核坐标空间。 |

## 必须 / 禁止

- **必须**：改瞄准 Y 之前用飞行日志的 Ap 掉落方向核对；注释写清「AbsPos：更大 Y = 更高」。
- **禁止**：看见 `219` 与 `-287` 符号相反就 `y = -y`。
- **禁止**：用 PetLoot / WY / 「枫之谷 Y 向上是减小」给 Travel / heli / Impact / 贴 NPC 的 Y 反号。
- **禁止**：未对照 `ReadLocalAp` 就把「该 Y 不是 AbsPos」写进代码。
