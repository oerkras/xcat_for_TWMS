---
description: 经典版 AbsPos/Y 轴方向——更大 Y=更高；禁止按传统 MS「更小 Y=上」改瞄准
alwaysApply: true
---

# AbsPos 坐标系（经典版 / TWMS）

本仓读写角色位置的真源是 **`VecCtrl.AbsPos`（Ap）**（Travel / heli / Impact / `ReadLocalAp` 等同口径）。

## 铁律（已用实机打穿）

| AbsPos | 含义 |
|--------|------|
| **Y 增大** | **更高**（远离地面 / 远离掉落方向） |
| **Y 减小** | **更低**（掉落方向） |

- 贴门「瞄准点抬高」= **`aimY += lift`**，再按需夹进触发框。
- **禁止**对 AbsPos 做 `aimY -= lift` 当「抬高」——会把点压到站立台下方 → 无限掉落。

### 证据（BIN · 2026-08-08）

- 错：`portal.y=-360` → `aimY-=24` → `aim=-389` → 人掉到 `ap.y=-735`（台下）。
- 对：抬高用 **增大** AbsPos.Y（见 `kPortalAimLiftY` / `ImpactStickToPortal`）。

## 易混点（不得混用）

| 口径 | 说明 |
|------|------|
| **AbsPos（本仓位移/贴门/旋翼）** | 上表；以实机掉落方向为准 |
| 传统 MS 地图 WY / 部分注释「更小 Y=上」 | **不要**直接套到 AbsPos 加减 |
| `worldY = -Ap.Y` 一类换算 | 仅显示/另一套空间；改 Ap 瞄准前先确认读的是不是 AbsPos |

`foothold_path` 里 ClimbUp「朝更小 Y」等图论表述若与上表冲突，**改 AbsPos 瞄准/冲量时以上表为准**；改图论边语义前先核对边的坐标空间。

## 必须 / 禁止

- **必须**：注释与设计文档写清「AbsPos：更大 Y = 更高」；抬高/压低用 `+=` / `-=` 时对照本表。
- **禁止**：凭「枫之谷 Y 轴向上是减小」在 Travel/heli/Impact 路径上对 AbsPos 做反向偏移。
- **禁止**：未对照 `ReadLocalAp` / 飞行日志掉落方向就改瞄准 Y 符号。
