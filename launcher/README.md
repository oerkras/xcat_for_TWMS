# 经典版一键启动器

## 用法

1. 运行 `bin\MscLauncher.exe`
2. 「账号串」粘贴：`邮箱----密码----其它…`（只使用前两项）——会自动保存，下次打开自动填入
3. 点 **一键启动游戏**

## 本地文件

| 路径 | 说明 |
|---|---|
| `%LocalAppData%\xcat_msc\account.txt` | 账号串（本机明文，勿外传） |
| `%LocalAppData%\xcat_msc\webview_profile` | WebView 登录态 |
| `bin\launcher.log` | 运行日志 |
