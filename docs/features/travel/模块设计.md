# travel Feature 模块设计（Classic TWMS）

> **状态**：✅ 同盘 seed BFS 赶路 + Impact 贴门进门（世界地图 Spot 入口）  
> **产品**：新枫之谷：经典版（TW · `Maplestory_Classic.exe`）· **不是**枫星  
> **源码**：`x/features/travel/` · `x/features/ports/travel_port.*` · `x/features/worldmap_marker_travel/`  
> **依赖**：`teleport_port::ImpactImpulseToward`（跟 F6）· `foothold_path::SnapStandForPortal` · `world_port` 场景门 · `invuln` 赶路 Hold · `fly::GetMode`  
> **对照枫星**：假火软确认 / 唯一桥 / 按门名重解 / PlayReady 闸——**仅模式对照**；偏移与进门以经典版为准  
> **明确非目标**：码头自动化、跨板块自动爬图、港口换乘脚本

---

## 0. 一句话

世界地图 Spot 双击 → 同盘 BFS 求路径 → 每跳 **Impact 冲量贴门** + CheckMove/↑ 进门；fill+Doing 已废。

---

## 1. 模块职责

| 模块 | 路径 | 做什么 |
|------|------|--------|
| **travel** | `x/features/travel/travel.{h,cpp}` | Goto 状态机、假火/唯一桥、重规划、无敌 Hold、磁盘 cmd、`PathHopCount` |
| **travel_graph** | `travel_graph.*` | seed/实测边、BFS、`IsUniqueBridgeName`、DEAD 复活 |
| **travel_port** | `ports/travel_port.*` | 枚举门、`CurrentMapKey`、`FirePortalByName`、贴门 `StickThenEnterReady` |
| **worldmap_marker_travel** | `worldmap_marker_travel/` | Spot 双击 → 预检 → YesNo / Notice → `RequestGoto` |
| **图数据** | `bin/XCat_data/state/travel_graph.seed.tsv`（+ 运行时 `travel_graph.tsv`） | 同盘边表 |

面板/UI 只经 `RequestGoto` / `RequestStop` / `QuerySnapshot`；worker 50ms tick。

ImGui「超级赶路」产品面：目的地 = 输入/开始/停止/填入已选；地图目录 = 大区跟盘 + 列表高亮当前图 + 搜索/重载/保存学习图。进门默认 `DirectEnter`（面板已不暴露 firemode/抓包探针）。

---

## 2. 产品路径（进门）

默认 `FireMode::DirectEnter`（可切 `StickUp`＝贴门+↑）：

```
FirePortalByName(name)
  → StickThenEnterReady(门)
       → 已在触发区 → OK（外层补一枪 CheckMove/↑）
       → ImpactStickToPortal
            → SnapStandForPortal → stand/fh（fh==0 → FH0_FORBID）
            → 需 invuln；否则 INVULN_OFF
            → 循环 ImpactImpulseToward（本循环 adaptive 近距软刹；不改 F6/战斗默认）
                 掠过 PortalRect → 当拍停 toward + CheckMove/↑（FIRED_*）
                 / 卸图 MAP_TRANSITION / 超时 NOT_STOOD / 连续失败 IMPACT_STICK_FAIL
  → 已 FIRED_* / MAP_* → 直接成功（勿二次 inRect，防残速 OUT_OF_RECT）
  → 仅 already_in 的 OK → 外层再 CheckMove/↑
```

调试旁路（非产品默认）：`Up` / `CheckMove`（当前位置触发，不硬写坐标）/ `Rpc`。
贴门只走 Impact；**禁止** AbsPos / Transform 硬钉；**禁止** fill+Doing。
`Transform.set_position` 解析与 TravelPos 锚灯已退役（产品不硬写坐标）。

枚举：优先 PortalManager；空则回退 `MapData.Portals@0x40`。

---

## 2.1 贴门钉台实现（`travel_port`）

源码：`StickThenEnterReady` / `ImpactStickToPortal` / `AlreadyStoodAtPortal`（`x/features/ports/travel_port.cpp`）。

### 落点策略（产品定案）

与战斗贴怪落点同源 Snap：先由 `PortalRect`/`HImpact` 得触发区，瞄准点夹进区内，再
`foothold_path::SnapStandForPortal(aim → stand,fh)`，再

`teleport::ImpactImpulseToward(standX, standY, route, opts)` 分段冲到门内。

| 约束 | 行为 |
|------|------|
| 触发区 | `MapPortalData.PortalRect`；空则 `±HImpact/VImpact` 兜底 |
| Snap 失败或 `fh==0` | 直接 `FH0_FORBID` |
| 冲量路线 | `fly::GetMode()`：0=NockBack，1=SetImpactNext（跟 F6） |
| 分段参数 | 对齐 F6 HopTo：`maxSegPx=320` / `minSegPx=8` / `maxSpeed=1600` / `speedScale=4` |
| 无敌 | `invuln::IsEnabled()`；赶路 `HoldInvulnForTravel`；否则 `INVULN_OFF` 硬停 |
| 超时 | `kImpactStickMaxMs≈10s` → `NOT_STOOD`（瞬态可重试） |

### 站稳判定

几何进门：`AlreadyStoodAtPortal`（strict rect，或无 rect 时近距 `kStickNearR`）。

- 几何 OK → 站稳，继续 CheckMove/↑
- 循环中卸图 / 非 PlayReady → `MAP_TRANSITION`（真换图，成功）
- 超时仍未进区 → `NOT_STOOD`；`IMPACT_STICK_FAIL` 连续冲量失败

### 与 Goto 的衔接

- `NOT_STOOD` / `IMPACT_STICK_FAIL` / `OUT_OF_RECT` ∈ 瞬态失败 → 可重试熔断
- `INVULN_OFF` → 立刻停赶路
- `FH0_FORBID` **不**进 transient；走假火软确认 / 重规划路径
- `MAP_TRANSITION` / `MAP_CHANGED`：`FirePortal` 返回 true，**跳过** CheckMove

---

## 3. Goto 状态机要点

1. **解析目标**：`map_names` 解析；无当前图 → `pending goto` 等 `CurrentMapKey`
2. **BFS**：`Graph::PathTo(src,dst)`；空则 revive DEAD 再试；仍空 → `Unreachable` + 通知
3. **开火**：解析 live 门名 → `FirePortalByName`（**放 `gMu`**）→ 记 `gExpectMap` / pending
4. **换图确认**：离开 `gPendingFrom` → `OnMapEnterConfirmed`（实测写边）
5. **到站**：仅 `cur == gTarget`；跳数用尽未到 → `ReplanOrStop`（禁止假 arrived）
6. **假火**：超时未换图 → soft 重试 → fuse 停（`FakeFireStop`）；唯一桥不标死、可 late-wait
7. **Shutdown / SetIdle**：清状态 + `ReleaseInvulnForTravel`；`++gFireEpoch`

### 3.1 时序护栏

| 常量 | 约值 | 用途 |
|------|------|------|
| `kHopWaitMs` | 1200 | 普跳等换图 |
| `kHopWaitUniqueBridgeMs` | 1500 | 唯一桥等换图 |
| `kMidHopSettleMs` | 1500 | 换图后稳图再开火 |
| Impact 贴门 | — | `ImpactImpulseToward`；无敌 Hold；无 fill+Doing |

硬门禁：

- `!IsInMapScene \|\| !IsPlayReady` → 禁止贴门冲量 / 禁止开火；midhop settle 时钟后推
- `SnapStandForPortal` 失败或 `fh==0` → `FH0_FORBID`（禁止贴门）
- 卸图中禁止 re-stick

### 3.2 开火放锁与 epoch

`FirePortalByName` 内含 Sleep/冲量，**禁止持 `gMu`**（否则 `QuerySnapshot` / Stop 卡死）。

```
fireEpoch = gFireEpoch
unlock → FirePortalByName → lock
若 mode≠Goto 或 epoch≠gFireEpoch → 丢弃提交（仅打 log）
```

`SetIdle` / 新 `StartGotoResolved` 递增 `gFireEpoch`。

### 3.3 贴门 / 进门结果语义（诚实）

| 结果 | 含义 |
|------|------|
| `FH0_FORBID` / `IMPACT_STICK_FAIL` / `NOT_PLAY_READY` / `INVULN_OFF` / … | **失败**，不得冒充换图成功 |
| `NOT_STOOD` | 冲量超时仍未进触发区；**瞬态**，可重试本跳 |
| `MAP_TRANSITION` | 开火后卸图 → `FirePortal` 返回 true，跳过二次 CheckMove |
| `MAP_CHANGED` | 贴门后 `CurrentMapKey` 已变 |
| `FIRED_*` / `OK` | 已开火或已在门内可继续 |

`IsTransientFireFail`：`IMPACT_STICK_FAIL`、`NOT_PLAY_READY`、`NOT_STOOD`、`OUT_OF_RECT`、`MAIN_TIMEOUT`…（兼容残留 `TELEPORT_*` 字符串）  
`FH0_FORBID` → 假火软确认路径；**不**当无限 transient 空转。

---

## 4. 世界地图入口

详见 **[`../worldmap_marker_travel/模块设计.md`](../worldmap_marker_travel/模块设计.md)**（锚点 / 字段偏移 / BIN）。

摘要：

1. Spot 双击 → 优先 mapId；否则名/简介经 `map_names` 反查（禁止把简介原文当 goto）
2. **`PathHopCount(cur, dst)` 预检**
   - 不可达 / 坏目标 / 已在目标 → 游戏内 `UIUtilDialog.Notice`（RVA `0x749420`），不弹赶路确认
   - 可达 → YesNo（RVA `0x7454B0`），文案含「预计 N 跳」
3. 确认 → `travel::RequestGoto`

launcher 气泡 / 历史事件：`NotifyTravelOutcome` → `PublishNotification` → launcher `eventlog::Record`（Unreachable / FakeFireStop / BadTarget / AlreadyThere / **FireStuck**）；不可达以游戏内 Notice 为主。

---

## 5. 失败与用户可见反馈

| FailKind | 典型触发 | 反馈 |
|----------|----------|------|
| Unreachable | 同盘无路径 | Notice（预检）+ launcher / 历史事件 |
| AlreadyThere | src==dst | Notice / Info |
| BadTarget | 解析失败 | Warning + 历史事件 |
| FakeFireStop | 进门多次未换图 | Danger + 历史事件 |
| FireStuck | 硬贴门失败持续 ≥8s（`IMPACT_STICK_FAIL` / `NOT_STOOD` / `FH0_FORBID` 等；**不含** `NOT_PLAY_READY`） | Danger + 历史事件（`travel.fire_stuck`） |

---

## 6. 关联文档与代码

- 世界地图入口详设：[`../worldmap_marker_travel/模块设计.md`](../worldmap_marker_travel/模块设计.md)
- 瞬移真源：[`../teleport/模块设计.md`](../teleport/模块设计.md) · [`../teleport/P0a_瞬移CALL锚点.md`](../teleport/P0a_瞬移CALL锚点.md)
- 场景门：[`../world_manager/SceneState与Field.md`](../world_manager/SceneState与Field.md)
- 无敌 Hold：[`../invuln/模块设计.md`](../invuln/模块设计.md)
- 地图名：`common/xcat_map_names.*`

---

## 7. BIN / 验收清单

1. 同盘可达 Spot → YesNo 显示预计跳数 → 确认后逐跳 `FIRED`，最终 `arrived`
2. 跨盘/无边 Spot → **Notice 不可达**，不启动 Goto
3. 已在目标 Spot → Notice「已在目标」，不赶路
4. 击退离门：snap 测距 → 至多重贴一次；卸图中无 re-stick 闪退
5. `fh==0` 门：log `FH0_FORBID`，不冲量贴门
6. **贴门日志必须** `impact stick aim … fh=<非0>`（或 `glide-fire`）；禁止再出现赶路贴门 `fh=0` 后 `ap` 穿台直坠
7. **FH AABB 外沿门**：如 `101030100/east01`，Snap 落点须仍在触发区内（或夹进区后再冲）
8. 假火满 fuse：停赶路 + `fake_fire` 通知；唯一桥不标死边
9. 关 payload / Shutdown：赶路无敌释放（`invuln release`）
10. 赶路中面板 Snapshot 不因开火 Sleep 长时间卡死（放锁）

### 7.1 钉台回归（2026-08-03）

| 项 | 证据 |
|----|------|
| 穿台失败 | `07:26` `east00`：`fh=0 snap=0`，`stand … ap=(1907,-805)` |
| 钉台通过 | `07:38` `50001→60001` / `60001→50001`：各跳 `snap=1`，无 `NOT_STOOD`，均 `arrived` |

### 7.2 边界闸误杀（历史 · fill+Doing 时代，2026-08-05）

| 项 | 证据 |
|----|------|
| 误杀 | 用户包 `…ddfc84`：`101030100` `land=(1176,-912) fh=76`，`src=fh B=-912` + margin24 → `out_of_bounds` → `east01 TELEPORT_FAIL` |
| 当时修复 | 贴门 Snap `snapStand && fh≠0` → margin=0（产品已改 Impact；此条仅作历史） |

> 构建产物：`bin/XCat_data/xcat.dll`（目标 `xcat_probe`）。占用中链接会 `LNK1104`，需自行关掉占用进程后再编。
