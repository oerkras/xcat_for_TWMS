# XCat 启动系统实现（经典版 · 从零重建指南）

> **TWMS / MapleStory Classic note (2026-07-30)**: Current target is **新楓之谷：經典版**（TW beanfun / Gamania Galaxy）。
> 枫星 / MapleStory Worlds（`xcat_for_fengxing`）的 NGM 编排可复用；**passarg 语义不可照搬**（见 §5）。
> 官网入口：[Main](https://maplestoryclassic.beanfun.com/Main) · Galaxy：`/webapi/view/login/mstc`。
> 逆向语料：`Dumps/ANALYSIS_NOTES.md`、`Dumps/runtime/out/dump.cs`。

> 目的：完整记录 **经典版 NGM 启动骨架** 的设计与实现边界，覆盖从 Galaxy 换票到 `ngm://` 拉起 `Maplestory_Classic.exe`。
> 当前主目标：**NGM 启动 + Classic LoadLibrary 注入探针**（完整 FEATURE payload 后续替换 `bin/XCat_data/xcat.dll`）。
> 范围：**GalaxyTicket → NGM deep-link → 等待游戏进程 → 等 GameAssembly → CRT LoadLibraryW**；入口 `bin/xcat.exe`。
> 2026-07-30：换票开游戏已验证；注入按枫星默认路径移植 `injector/classic_loadlibrary`，探针 DLL `x/probe` → `xcat.dll`。禁止默认 manual-map / stealth。

---

## 1. 这个系统要解决什么

把经典版客户端 `Maplestory_Classic.exe` 用 **与官网「启动游戏」等价的路径** 拉起来：

- 会话票来自 Gamania Galaxy / beanfun OpenID，**不是** Nexon `/sid:`。
- 官网流程：`OTT` → `POST /api/Login/GetOneTimeWebInfo` → `NGM.LaunchGameOnlyPassArgs(...)`。
- 我们采用同一套 **NGM deep-link 的 `-passarg:`**，把换票四元组透传给 NGM，由 NGM 启动客户端。

对照枫星（老项目）时务必分清：

| | 枫星 / MSW | 经典版 / MSC |
|---|---|---|
| 进程 | `msw.exe` | `Maplestory_Classic.exe` |
| 鉴权源 | Nexon NGM 登录 → cmdline `/sid:` | Galaxy OTT → `userSessionToken` 等 |
| passarg | `/runmode /gameid /publishid /gamename /publishname` | `userObjectID userSessionToken gid galaxy_GameId` |
| `-game:` | 固定 `106603` | API 字段 `game`（实机钉死前勿硬编码） |
| 造不出来的东西 | `/sid:` | `userSessionToken` / 有效 `OTT` |

官网启动页与脚本真源：

- [maplestoryclassic.beanfun.com/Main](https://maplestoryclassic.beanfun.com/Main)
- `/web/js/components/float-btns.js`（`gamePlay` / `ottLoginApi` / `NGM.LaunchGameOnlyPassArgs`）
- `/web/js/api.js` → `GetOneTimeWebInfo`
- `https://platform.nexon.com/NGM/JS/npf_ngm.js`

---

## 2. 组件与目录

| 组件 | 路径 | 形态 | 角色 |
|---|---|---|---|
| **启动核心** | `launcher/msc_launch.h` + `msc_launch.cpp` | 静态库 `msc_launch` | GalaxyTicket、deep-link、NGM 拉起、等进程 |
| **HTTP 取票** | `launcher/http_client.*` + `http_beanfun_login.*` | 同库 | WinHTTP Cookie：beanfun(HK) → OTT → GetOneTimeWebInfo（虚拟机默认） |
| **一键换票会话** | `launcher/msc_webview_login.*` | 同库（链进 `xcat.exe`） | 策略：`HTTP优先` / `仅HTTP` / `仅WebView`；WebView 为回退 |
| **独立换票窗（可选）** | `launcher/msc_launcher_app.cpp` | `MscLauncher.exe`（`XCAT_BUILD_MSC_LAUNCHER_EXE`） | 调试用；默认不编 |
| **设计文档** | `docs/features/ops/启动系统实现.md` | 本文 | 从零重建与边界 |
| **逆向笔记** | `Dumps/ANALYSIS_NOTES.md` | 笔记 | 客户端锚点、GHome / GalaxyBeanfunBridge |
| **产品 GUI** | `xcat_app/` | `xcat.exe` | 账号串 + 取票策略 + 一键；功能 Tab |
| **注入** | `injector/classic_loadlibrary.*` + `inject_after_launch.*` | 静态库 `xcat_inject` | CRT LoadLibraryW |
| **探针 DLL** | `x/probe/xcat_probe.cpp` | `bin/XCat_data/xcat.dll` | 注入存活探针（待换完整 payload） |

依赖关系（目标态，非现状）：

`xcat.exe` → `launcher` →（可选）`xcat_inject_lib` + `imgui_lib`。

现状目录：

```
xcat_for_TWMS/
├── launcher/              # NGM 启动骨架（msc_launch.*）
├── docs/features/ops/     # 运维与启动设计（本文）
├── Dumps/                 # dump.cs / opcode / 安全层笔记
└── （未建）xcat_app/ injector/ common/ bin/
```

---

## 3. 关键数据结构（`launcher/msc_launch.h`）

```cpp
enum class Stage {
    Init,
    FindingNgm,
    LaunchingGame,
    WaitingForGame,
    Done,
    Failed,
    BlockedNeedsTicket,  // 缺 OTT/换票结果
};

enum class NgmMode {
    Launch,   // 官网 LaunchGameOnlyPassArgs 默认
    Restore,  // 枫星现行；经典版待实机验证
};

struct GalaxyTicket {
    std::wstring userObjectId;      // ottRes.data.userObjectID
    std::wstring userSessionToken;  // ottRes.data.userSessionToken（短命，勿落盘明文）
    std::wstring gid;               // ottRes.data.gid
    std::wstring galaxyGameId;      // ottRes.data.galaxy_GameId
    std::wstring ngmGameId;         // ottRes.data.game → -game:'...'
};

struct Options {
    GalaxyTicket ticket;
    NgmMode      mode = NgmMode::Launch;
    std::wstring gameExeName = L"Maplestory_Classic.exe";
    int          waitGameSec = 90;
    bool         dryRunDeepLinkOnly = false;  // 只生成 deep-link 摘要
};

struct Progress {
    Stage         stage;
    std::string   message;
    unsigned long gamePid;
};

struct Result {
    bool          ok;
    Stage         finalStage;
    std::string   errorMessage;
    unsigned long gamePid;
    std::string   deepLinkSummary;  // 仅脱敏摘要；禁止回传明文 deep-link
};
```

`TicketLooksUsable()`：五段非空，且拒绝 `'` / `"` / 空白 / 控制符 / `\`（防 `-passarg:'...'` 截断）。

`GalaxyTicket` 字段与官网 `float-btns.js` 对齐：

| 字段 | 官网 JSON | 用途 |
|---|---|---|
| `userObjectId` | `data.userObjectID` | passarg 第 1 段；客户端 `Galaxy_UserObjectID` |
| `userSessionToken` | `data.userSessionToken` | passarg 第 2 段；`Galaxy_UserSessionToken` |
| `gid` | `data.gid` | passarg 第 3 段 |
| `galaxyGameId` | `data.galaxy_GameId` | passarg 第 4 段 |
| `ngmGameId` | `data.game` | NGM `-game:'...'`（**不是**四元组里的 galaxyGameId） |

非法或缺失 → `BlockedNeedsTicket`（对应老项目「请确认 NGM 已登录」）。

---

## 4. Run() 流水线（`launcher/msc_launch.cpp`）

```
0. 参数校验           TicketLooksUsable（非空+字符白名单）；否则 BlockedNeedsTicket
1. 找 NGM             ProgramData\Nexon\NGM\NGM64.exe 等候选 + ngm:// 注册表
2. 生成 deep-link     栈上 BuildNgmDeepLink；Result 只留 FormatDeepLinkForLog 摘要
3. dry-run？（可选）   dryRunDeepLinkOnly → Done（不拉进程）
4. 快照 PID           CollectPidsByExeName(gameExeName) → before 集合
5. EnsureNgmRunning   冷机先拉 NGM；失败仍继续 deep-link
6. 启动游戏           ShellExecute(ngm://) → 失败则 CreateProcess(NGM64, deep-link)
                      失败路径带 Shell/CreateProcess 错误码
7. 等待验票           WaitForClassicWithTicket：新 PID ∧ cmdline 四元组 == ticket
8. Done               返回 gamePid + deepLinkSummary + cmdLineSummary
                      （未做）登录 UI/ACS 门闩 / 注入
```

### 4.1 与老项目流水线对照

| 步骤 | 枫星 `launcher_run.cpp` | 经典版骨架 |
|---|---|---|
| 找游戏 | 找 `msw.exe` 安装路径 | 暂不强制；交给 NGM |
| 联网配置 | publishId HTTPS 刷新 | OTT / GetOneTimeWebInfo（**未接**；票由外部填入） |
| 已在跑检测 | cmdline `/sid:` + gameid/publishid 匹配 | 拉起前 PID 快照；**不**把旧进程当成功 |
| 启动 | EnsureNgm → deep-link → .url → 裸 ngm | EnsureNgm → deep-link → CreateProcess（**无** .url/裸 ngm） |
| 鉴权就绪 | `WaitForMswWithAuth`（等 `/sid:`） | `WaitForClassicWithTicket`（等位置参数四元组） |
| 切服 | kill+restart 改 `/publishid:` | N/A（无 UGC 世界切服） |
| 注入 | Classic LoadLibraryW | **明确不在本骨架** |

### 4.2 注入方式决策（预留 · 未实施）

规划对齐老项目 2026-06-20 实机结论：**默认只走 Classic `LoadLibraryW`**。

```text
VirtualAllocEx(remote dll path)
→ WriteProcessMemory(path)
→ CreateRemoteThread(kernel32!LoadLibraryW)
→ payload DllMain
```

在取得本客户端 `Maplestory_Classic` + `Msc.Security` / GRAP 的注入存活证据前：

- **禁止**把 manual-map / stealth / spawn 当作默认路径写进产品。
- 经典版另有 `Msc.Security`（CreateFileW / DeviceIoControl），爆炸半径可能大于纯 MSW；注入单独立项。

### 4.3 敏感信息铁律

- `userSessionToken` / 完整 `-passarg` / 完整 deep-link：**禁止**写入 `Result`、日志或仓库。
- `Result.deepLinkSummary` 与进度回调只允许 `FormatDeepLinkForLog`（`-passarg:'***'`）。
- deep-link 明文仅存在于 `Run` 栈上临时变量，函数返回后不再对外可见。
- OTT 与 SessionToken 均短命；过期必须重新走 Galaxy，禁止当长期免登凭证缓存。

---

## 5. NGM deep-link 格式

官网等价（`NGM.LaunchGameOnlyPassArgs` → `GenerateArgument` + `GenerateURI`）：

```
ngm://launch/ -mode:launch -game:'<data.game>' -passarg:'<userObjectID> <userSessionToken> <gid> <galaxy_GameId>' -architectureplatform:'none' -timestamp:<ms>
```

骨架实现：`BuildNgmPassarg` + `BuildNgmDeepLink`。

说明：

- `-mode:launch`：与官网 JS 默认一致。`-mode:restore` 在枚举中预留（枫星现行），**经典版是否更稳需实机对比**，不得凭枫星经验直接改默认。
- `-game:'…'`：来自换票 API 的 `game` 字段；在实机钉死常量前保持运行时传入。
- `-passarg:`：四段空格分隔，交给 NGM；**NGM 再拆成游戏进程位置参数**（见 §5.2）。
- 系统协议注册（本机实锤）：`HKLM\Software\Classes\ngm\Shell\Open\Command` → `"C:\ProgramData\Nexon\NGM\NGM64.exe" "%1"`。

### 5.1 枫星 deep-link（对照，勿混用）

```
ngm://launch/ -mode:restore -game:106603 -passarg:'/runmode:play /gameid:... /publishid:... /gamename:... /publishname:...'
```

### 5.2 游戏进程 cmdline 实锤（2026-07-30）

本机 `Maplestory_Classic.exe` 经 `NtQueryInformationProcess(ProcessCommandLineInformation=60)` + `PROCESS_QUERY_LIMITED_INFORMATION` 读到：

```text
"G:\Games\maplestory_classic\Maplestory_Classic.exe" <userObjectID> <userSessionToken> <gid> <galaxy_GameId>
```

采证形态（token 打码）：

```text
"…\Maplestory_Classic.exe" 4662771 sess5d*** 2373 944
```

| argv | 语义 |
|---|---|
| `[0]` | exe 路径（常带引号） |
| `[1]` | `userObjectID` |
| `[2]` | `userSessionToken`（日志只留前缀） |
| `[3]` | `gid` |
| `[4]` | `galaxy_GameId`（与 Galaxy OTT 中的 `944` 一致） |

要点：

- **不是** `-passarg:'…'` 留在游戏 cmdline；也**不是**枫星 `/sid:` / `/gameid:`。
- PEB `ReadProcessMemory` 曾 `ERROR_ACCESS_DENIED`；读 cmdline 用 **class 60**，不要只抄枫星 PEB 路径。
- 就绪门闩：`WaitForClassicWithTicket` = 新 PID ∧ 四段全等匹配。
- 脱敏：`FormatCmdLineForLog` → `"exe" uid token*** gid galaxyId`。

---

## 6. 换票链路（官网 · 本骨架的票源）

开游戏本身**不需要** WebView；需要的是 `GalaxyTicket`。默认策略为 **HTTP 优先**（虚拟机无 WebView2 Runtime 亦可换票）。

### 6.1 HTTP 模拟登录（默认）

```
StartOneClick（策略 HttpFirst / HttpOnly）
→ GET galaxy…/login/mstc → sessionOtt（/login/init/mstc/OTT:…，仅供 result POST）
→ GET bfweb.hk.beanfun.com/.../bflogin/default.aspx?service=610076_T0&url=…/login/result/mstc/ghk
→ login.hk.beanfun.com/login/id-pass_form_*.aspx?otp1=…
→ POST 账密（记住帐号）→ AuthKey / WebToken
→ GET Galaxy result?WebToken=（可选抽 CSRF）
→ POST result/mstc/ghk?WebToken=  body {ott:sessionOtt, fromSelf:true[, _token]}
   → Results.Ott = ticketOtt（OTT:944:Login:…）
→ POST /api/Login/GetOneTimeWebInfo { OTT=ticketOtt }
→ GalaxyTicket → NGM.LaunchGameOnlyPassArgs / Run() + 注入
```

日志区分：`sessionOtt=`（Galaxy init）与 `ticketOtt=`（换票）；POST 失败会打 `status / RCode / Message / body_snip`。

边界：

- 遇 **驗證碼 / reCAPTCHA / 雙重驗證**：HTTP **明确失败**（不打码）；`HttpFirst` 且本机已装 WebView2 Runtime 时按「验证码UI」回退。
- `HttpOnly`：不回退、不强制弹 Runtime 安装。
- HTTP 忙碌看门狗约 **5 分钟**（与 WebView 180s OTT 定时器分离）。
- 落点：`launcher/http_client.*`、`http_beanfun_login.*`；配置：`bin/auth_strategy.txt`、`bin/captcha_ui.txt`（与 `account.txt` 同级）。

### 6.1.1 验证码验收清单（人工）

| 步骤 | 期望 |
|---|---|
| 干净账号一键 | 日志 `sessionOtt` → `ticketOtt` → `HTTP 换票成功`；**无**登录弹窗 |
| 触发验证码账号 | 状态栏「需要网页验证」+ 通知；默认策略弹出登录窗或开浏览器 |
| 验证码UI=仅浏览器 | 不弹登录窗，只开官网 |
| 验证码UI=静默回退 | 不弹窗、不开浏览器；HttpFirst 仍可静默 WebView |
| 无 WebView2 + HttpFirst | 验证码时开浏览器；不卡死 |

### 6.2 WebView 路径（回退 / 仅 WebView）

```
用户点 #gamestart（或内嵌 WebView 一键）
→ GalaxyLogin = https://galaxy.games.gamania.com/webapi/view/login/mstc?redirect_url=…/Main
→ beanfun OpenID（clientid=17599671-8530-4a00-9825-1c2b346eab4d）
→ 回跳 Main?OTT=OTT:944:Login:…
→ POST /api/Login/GetOneTimeWebInfo { OTT }
→ GalaxyTicket
→ NGM.LaunchGameOnlyPassArgs / 本骨架 Run()
```

客户端岔路（无网页票时）：

- `UIGalaxyLoginWebView`（Vuplex）+ `GalaxyBeanfunBridge`
- 可先在客户端完成登录，再只做冷启/注入；骨架用 `BlockedNeedsTicket` 显式拦住「无票硬拉」。

### 6.3 取票策略（启动页）

| 策略 | 行为 |
|---|---|
| `HTTP优先`（默认） | 先 HTTP；失败且有 Runtime → WebView |
| `仅HTTP` | 只走 HTTP；无 Runtime 也能用 |
| `仅WebView` | 旧行为；缺 Runtime 才提示安装 |

### 6.4 验证码 UI（启动页）

| 模式 | 行为 |
|---|---|
| `验证码弹窗`（默认） | 验证码时弹互动登录窗；无 Runtime 则开浏览器 |
| `仅浏览器` | 永不弹登录窗，只开官网 |
| `静默回退` | 不弹窗、不开浏览器；能回退则静默 WebView |

## 7. GUI 设计（预留）

对齐老项目 `xcat_app/`，**尚未创建**：

- 独立 Win32 + D3D11 + ImGui
- `launch_panel` / `launch_worker` 调 `msc::launcher::Run`
- 主按钮文案建议：**启动**（无注入）→ 日后 **启动并注入**
- 缺票时 UI 应显示可操作指引（打开官网 / 打开客户端 WebView），不要静默失败

---

## 8. 从零重建步骤

1. 确认本机 `ngm://` 可用、`Maplestory_Classic.exe` 已安装（例：`G:\Games\maplestory_classic\`）。
2. 落地 `launcher/msc_launch.{h,cpp}`（已完成骨架）。
3. 实机抓一次 `GetOneTimeWebInfo` 响应（token 打码）→ 钉 `ngmGameId` 与字段表。
4. 实机抓一次 `GetOneTimeWebInfo` 响应（token 打码）→ 钉 `ngmGameId` 与字段表；或用 `msc_launch_smoke --ott ...`。
5. （可选）`--ott` 换票后 `--launch` 真拉起，核对 `cmdLineSummary`。
6. 试 `NgmMode::Restore` vs `Launch`，记入本节修订。
7. CMake 产品化 GUI +（证据足够后）Classic LoadLibrary 注入。

本地冒烟：

```text
cmake -S launcher -B launcher/build -G "Visual Studio 17 2022" -A x64
cmake --build launcher/build --config Release
launcher\build\Release\msc_launch_smoke.exe
launcher\build\Release\msc_launch_smoke.exe --ott "OTT:944:Login:..."
```

---

## 9. 后续扩展点（预留）

| Feature | 建议落点 |
|---|---|
| 仅生成 deep-link | `Options.dryRunDeepLinkOnly = true` |
| OTT → 换票 HTTP | `launcher/ott_ticket_fetch.*`（`FetchGalaxyTicketFromOtt`） |
| beanfun HTTP 登录 | `launcher/http_beanfun_login.*` + `http_client.*`（默认策略） |
| cmdline 验票就绪 | `WaitForClassicWithTicket` + `GetProcessCommandLineW`(class 60) |
| attach 已有进程 | 跳过启动，只做后续注入 |
| GUI | `xcat_app/launch_*`，独立于启动核心 |
| 注入 | `injector/classic_loadlibrary.*`；默认不扩展 manual-map |
| 脏会话清栈 | 对标枫星 `ResetNgsSessionForRelaunch`；经典版需单独验证 NGM/NGS 行为 |

---

## 10. 故障排查

| 现象 | 处理 |
|---|---|
| HTTP 登录遇验证码 | 文案明确失败；`HTTP优先`+有 Runtime → 自动 WebView；否则装 Runtime 或改策略 |
| 未找到 NGM | 确认 `C:\ProgramData\Nexon\NGM\NGM64.exe` 或 `ngm://` 注册表 |
| ShellExecute 失败 | 看 CreateProcess 回退是否成功；查协议处理程序 |
| 等待新游戏超时 | 票过期、未装经典版、NGM 未绑定该 `-game:`，或仅有拉起前旧进程 |
| 把旧登录页当成功 | 已用 PID 快照防护；若仍误判查是否 PID 复用（极少） |
| 新进程在但仍停登录页 | passarg 未吃到或票无效；补 cmdline / WebView 状态门闩（TODO） |
| Ticket 被拒（非法字符） | 检查字段是否含引号/空白；以 API 原始值为准勿手改 |
| 日志里出现明文 token | **缺陷**：改走 `FormatDeepLinkForLog`，轮转并删除含密日志 |
| 误用枫星 `/gameid:/publishid:` | 经典版不会认；必须用 Galaxy 四元组 |

---

## 11. 相关文件索引

| 文件 | 职责 |
|---|---|
| `launcher/msc_launch.h` | Stage / GalaxyTicket / ClassicPassArgs / Options / API |
| `launcher/msc_launch.cpp` | deep-link、`GetProcessCommandLineW`(60)、`WaitForClassicWithTicket`、`Run` |
| `launcher/ott_ticket_fetch.{h,cpp}` | `FetchGalaxyTicketFromOtt`（WinHTTP → GetOneTimeWebInfo） |
| `launcher/http_client.{h,cpp}` | Cookie 罐 + GET/POST（WinHTTP，可控重定向） |
| `launcher/http_beanfun_login.{h,cpp}` | HK beanfun `610076_T0` → OTT → 换票 |
| `launcher/msc_webview_login.{h,cpp}` | AuthStrategy + HTTP/WebView 一键会话 |
| `launcher/msc_launch_smoke.cpp` | 本地冒烟 + 可选 `--ott` / `--launch` |
| `launcher/CMakeLists.txt` | `msc_launch` 静态库 + smoke exe |
| `launcher/README.md` | 骨架编译备注 |
| `docs/features/ops/启动系统实现.md` | 本文 |
| `docs/features/README.md` | features 索引 |
| `Dumps/ANALYSIS_NOTES.md` | 客户端逆向摘要（含启动骨架指针） |
| `Dumps/runtime/out/dump.cs` | `Galaxy_*` / `UIGalaxyLoginWebView` / GHome |
| 老项目对照 | `xcat_for_fengxing/docs/features/ops/注入器启动系统实现.md` |
| 老项目代码对照 | `xcat_for_fengxing/launcher/{launcher_run,process_util}.cpp` |

---

## 12. 非目标

- 伪造 / 绕过 Galaxy·beanfun 鉴权
- 持久化明文 `userSessionToken` / OTT
- 把枫星 `/sid:` 或 `/publishid:` 语义硬套到经典版
- 在本骨架内默认启用 manual-map / stealth 注入
