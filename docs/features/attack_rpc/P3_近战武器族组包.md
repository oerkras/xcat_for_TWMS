# attack_rpc P3 · 近战武器族组包（地面）

> **状态**：✅ 代码落地（`attack_rpc_port.cpp`）· 短刀 / 双手剑地面已 BIN 打穿 · 其余近战族逻辑已按族填、**掉血未验收**  
> **日期**：2026-08-16  
> **产品**：经典版 TWMS（`Maplestory_Classic.exe`）· **不是**枫星  
> **IDB**：`Dumps/runtime/GameAssembly.dll.i64` · imagebase **`0x7FFD72820000`**（与 P0c 旧 dump `0x7FFB74A20000` 勿混 VA）  
> **上级**：[`模块设计.md`](模块设计.md) · [`P0c_攻包BODY布局.md`](P0c_攻包BODY布局.md) · [`P2_物落脚下.md`](P2_物落脚下.md)  
> **入口**：吸怪 TAB「出刀自组攻包（钉锁）」`simpleCombatForgeHit` → `TryFireLockOid`  
> **红线**：Forge 开着失败**不得**退 `OnFuncKey`；不改短刀 `03 05`（除非短刀 BIN 坏了）；双手剑速度字节不得谎报成 3

---

## 0. 结论先行

1. 自组只发 **opcode 50** 近战普攻。弓/弩/拳套/枪（51）、杖（52）拒组（`op_not_melee`），调用方停这一刀，不退 OnFuncKey。  
2. **不能**全武器套短刀包。短刀头是 `WAS + 恒 05`（BIN `03 05`）；单手 WAS=5 套同一条会变成 `05 05`，服端当双手剑 AttackType 空挥。  
3. 官方 `TryDoingMeleeAttack`（RVA `0x105AE40`）头两字节永远是 **`UserLocal+0x158`（AttackType）+ GetAttackSpeedDegree**。短刀自组与官方序相反，但能伤，**保持例外**。  
4. 地面环按兵器长短分：短兵器写**人脚下**（落物，短刀 BIN）；长兵器写**怪坐标**（双手剑 `send.log`）。飞行怪环（`PosPrev@0x6C`）**先搁置**，不再用 `fly` 改 XY。  
5. 本地 `SetAttackAction` 可用 spd=3 看起来快；**线上第二字节必须写真 degree**。双手剑写成 3 → BIN 100→99 磨血；`05 06` + Delay `C2 01` 才是 100→60。

---

## 1. 锚点（现网）

| 项 | 值 |
|---|---|
| `UserLocal.TryDoingMeleeAttack` | RVA `0x105AE40` · VA `0x7FFD7387AE40` |
| Create opcode 种子 | `word ^ 0x668E` → **50** |
| AttackType | `MsAvatar+0x158`（CMS `+0x140`，本 dump +0x18） |
| WeaponAttackSpeed | `+0x15C` |
| 线上 degree | `clamp(WAS + nBooster@SS+0xBC [+ PartyBooster], 2, 10)` |
| 命中环第一点 | Mob getter → `EncodeVector2` |
| 命中环第二点 | `Mob+0x6C` = `VecCtrlOwner.PosPrev` |
| wire Y | `EncodeVector2` 内部 IEEE 翻号 ≡ `i16(x), i16(-AbsPos.y)` |
| 发包 | `Network.SendOutPacket`（facade），禁止 `Session.Send` 旁路 |

CMS `WeaponType`：30–33、37–49（无 34–36）。`ActionType`：SwingO1=5、SwingT1=9、SwingP1=13、StabO1=16。

TW 短刀 BIN：`atkType=1`（不是 CMS 常见的 4）、`WAS=3`。

离线表（`G:\Games\maplestory_classic` Character WZJS，2026-08-16）：

| 样本 | wt | afterImage | attackSpeed | 自组头 |
|---|---:|---|---:|---|
| `1302000` 劍（新手） | 30 | swordOL | **4** | `01 04` |
| `1302001` | 30 | swordOL | 5 | `01 05`（P0c 样例） |
| wt=30 全表 | 30 | swordOL 27 / swordOS 41 | 4×21、5×47 | 不要套短刀 `WAS+05` |
| `1332000` 短刀 | 33 | swordOL | 4 | 自组仍走 BIN `03 05` |
| wt=33 全表 | 33 | swordOL/OS | **3×14**（BIN WAS=3）、4×45、5×6 | |
| `1402000` 双手剑 | 40 | swordTS | 6 | `05 06` |

TW **没有** `dagger` afterimage；短刀跟单手剑共用 `swordOL/OS`，所以 `+0x158` 都是 1。动作 SwingO1=5（`action_delay_base.tsv` swingO1 三帧 800）。Delay 公式与双手剑 WAS=6→450 同一条：`(tier+10)*450>>4`。短刀 Delay 仍钉 `A5`（BIN 不跟 WAS=3 公式）。

---

## 2. 地面族表（实现真源）

代码：`x/features/ports/attack_rpc_port.cpp`  
`WeaponIsDagger` / `WeaponIsOneHandSwing` / `WeaponIsTwoHandFamily` / `FallbackAttackType` / `WeaponRingAtMob` / `WeaponForeAction` / `WeaponHitDelay` / `ResolveWireAttackType`。

| 族 | `fam=` | wt | 头（hdr 两字节） | 动作 | 环 | Delay | ForeAction | 验收 |
|---|---|---|---|---|---|---|---|---|
| 短刀 | `dagger` | 33 | **WAS + 恒 `05`**（`03 05`） | 5 | 脚下 | `A5 01` | `81` | ✅ BIN 连杀 |
| 单手剑/斧/钝 | `1h` | 30–32 | AttackType + 真 degree（`1302000`→`01 04`；WAS=5→`01 05`） | 5 | 脚下 | `(deg+10)*450>>4`（4→393，5→421=`A5`） | `81` | 🔍 头/Delay 已按离线表改，掉血 NOT RUN |
| 徒手 | `bare` | 39 | 同单手，回退 AT=1 | 5 | 脚下 | 同单手公式 | `81` | 🔍 NOT RUN |
| 指节 | `knuckle` | 48 | 同单手，回退 AT=13 | 5 | 脚下 | 同单手公式 | `81` | 🔍 NOT RUN |
| 双手剑/斧/钝 | `2h` | 40–42 | AttackType + 真 degree（剑真包 `05 06`） | 9 | 怪坐标 | `C2 01` | `00` | ✅ 双手剑地面大跳；斧/钝 NOT RUN |
| 枪/矛 | `pole` | 43–44 | 回退 AT 8/9 + 真 degree | 13 | 怪坐标 | `C2 01` | `00` | 🔍 只写了分支 |

不组包：杖 37、棍 38（52）；弓 45、弩 46、拳套 47、枪 49（51）。

AttackType 回退（`+0x158` 读不到，或读成和 WAS 同一个数且不等于回退值时）：

| wt | 回退 |
|---:|---:|
| 30 / 33 / 39 | 1 |
| 31 | 2 |
| 32 | 3 |
| 40 | 5 |
| 41 | 6 |
| 42 | 7 |
| 43 | 8 |
| 44 | 9 |
| 48 | 13 |

`at == was && 回退≠at` 是为挡单手 `05 05`。双手剑 `at=5, was=6` 不相等，不会被改成别的。

动作优先读 `SkillInfo.MeleeAttackAction[wt]` 静态字典（`+0x118`）；读不到用上表。实机日志常见 `via=fb`。不轮砍（BIN：双手剑轮 9/10/11 仍只前几刀有伤）。

---

## 3. BODY 布局（与 P0c 实机修正一致）

```text
01 11 | skill0×2 | bool0 | action u16 | hdr0 hdr1 | tOrKey u32
| oid | 06 | FA | frame00 | 01 | xy1 | xy2 | Delay u16 | dmg i32 | field0 | 玩家 xy
```

| 字段 | 短刀 BIN | 双手剑 `send.log` |
|---|---|---|
| action | `05` / 左向 `8005` | `09`/`0A`/`0B`/`10`/`11`，左向高位 `80` |
| hdr | `03 05` | `05 06` |
| FA | `81` | `00`/`06`/`80`/`86`（自组写 `00` 仍能伤） |
| xy | 人脚下，两枚相同 | 怪位，两枚可差 0–5px |
| Delay | `A5 01` = 421 | 常见 `C2 01`=450，也有 `5E 01`=350 |

`tOrKey` = `WorldManager.GetUpdateTime`。fieldId 恒 0。Y 必须翻号（P2）。

自组两枚 XY 目前写同一点（没有 `PosPrev`）。地面行走怪够用；飞行怪官方第二点是 `+0x6C`，另开任务。

---

## 4. 已证伪（不要再争）

| 说法 | 事实 |
|---|---|
| 双手剑自组没发出去 | 有 `op=50`；服端不认伤 |
| 双手剑慢是发包间隔 | 间隔与短刀同量级；慢在 WAS=6 认伤次数。谎报 `05 03` → 磨血 |
| 短刀头组错了 | 短刀 `03 05` 能连杀，勿改 |
| 单手也可以套短刀头 | WAS=5 → `05 05` 空挥 |
| 飞行空刀只因为写了脚下 | 改成怪坐标仍空刀；官方第二点是 PosPrev。地面先不跟 |

`+0x158` 是 AttackType，不是攻速。短刀 BIN `atkType=1`、`WAS=3`。误把 0x158 当攻速：短刀能伤、双手剑空枪。

---

## 5. 日志怎么验

`AttackRpc`：

```text
melee encode fam=… wt=… atkType=… rawAt=… ring=feet|mob delay=… fa=0x…
forge BODY … hdr=XX YY … ring=… hex=…
```

`combat.log`：`forge_hit ok … wt=… hp=`

| 武器 | 应看到 |
|---|---|
| 短刀 | `fam=dagger hdr=03 05 ring=feet`，hp 连掉 |
| 单手剑 | `fam=1h hdr=01 04`（新手剑 WAS=4）或 `01 05`（WAS=5），**不要** `05 05` / `04 05` |
| 双手剑 | `fam=2h hdr=05 06 ring=mob`，Delay `C2 01`，hp 大跳不是 100→99 |

`forge_hit fail … (no OnFuncKey)` 是故意的：过远 / 非近战 / SendOut false 停刀，不走正路。

---

## 6. NOT RUN / 非目标

- 单手剑/斧/钝、徒手、指节、枪矛：组包已按族填，**换武器掉血未打**  
- 双手斧/钝：套双手剑环/Delay，未单独 BIN  
- 飞行怪：不改环；官方 `PosPrev` 另开  
- PartyBooster 未加进 degree（只加了 `nBooster@0xBC`）  
- 远程 51 / 魔法 52 自组  
- 把伤害数字做成「必杀」（`SetDamage` 研究用，产品不承诺绕过服端）
