# map_attack P2 BIN · 2026-08-14 23:03

> **产品**：经典版 TWMS · 本机 `bin/XCat_data/logs` + `Dumps/runtime/kick.log`  
> **图**：`50001` · 角色 `job=0 wt=30` 单手剑 · A 空绑 `t=5 v=52` · `path=OnFuncKey`  
> **结论**：**客户端扩盒成功；服端拒远距近战。近战全图 NA 结案不可做。禁止回退造包。**

关联：[P2_扩盒.md](./P2_扩盒.md) · [P1_BIN.md](./P1_BIN.md)

## 0. 开关时间线

| 时 | 事件 |
|---|---|
| 23:01:58 | DLL `port init (P2 expand AABB, keep maxCount)` |
| 23:03:06.552 | `enabled=1` · `HitPin arm=1` VA `00007FFD617C77F0` RVA `0xF877F0` |
| 23:03:09.087–23:03:32 | 扩盒出刀（第一窗） |
| 23:03:32.232 | `enabled=0` |
| 23:03:32.542 | 关开关后 **310ms** 近距 `lastHitted` 恢复 |
| 23:04:09.819 | 再开 `enabled=1`（第二窗，角色已在图左 `x≈300`） |

`kick.log`：扩盒期间心跳 `state=3 sawDisc=0`，无 Disconnect / KickSniff。未踢号。

## 1. FindHit 入参（75 刀全中）

`x.jsonl` tag=`MapAtk` 且 `exp=`：**75/75**。

| 项 | 值 |
|---|---|
| `exp` / `why` | **75/75 = 1 / aabb** |
| `mc` | **75/75 = 1** |
| `n` / `no` | **75/75 = 1**（没有一刀两只） |
| 新盒 `xywh` | **唯一** `135,-450,1890,745` |
| 对照 `MapBounds` | `L=135 T=-450 R=2025 B=295` → w=1890 h=745，**对得上** |
| 旧盒 `orig` w×h | 仍是 P1 五档：64×15 / 70×56 / 71×14 / 78×67 / 80×40 |

盒写进去了。没抬 `maxCount`。

## 2. 名单打到了谁

| oid | 刀数 | `hypot(dx,dy)` | 位置（mobscan） |
|---|---|---|---|
| `4659183` | 3 | 0–65 | 身边，`y≈-365` |
| `4713276` | **72** | **542–1605** | 上层 `tpl=1210102`，`y≈175`（AbsPos 更大 Y=更高，Δy≈+542） |

第二窗角色走到 `x≈330` 时：`oid0=4713276 dx=1511 dy=542`。FindHit 在全图盒 + `mc=1` 下钉死池子第一只（mobscan `[0]` 一直是这只蜗牛），不是 F5 锁的那只。

## 3. 服端伤：远的不掉，近的也被偷刀

**远距目标 `4713276`**：23:03:14 → 23:04:09 连续 mobscan **hp=100%**。72 刀写进 FindHit 名单，血不动。

**近距锁目标**（F5 `fire id=` 与 `oid0` 对不上）：

- 仅头两刀 `oid0=4659183` 且 `|dx|<80`：`hp=100→42→dead`，`lastHitted` 跳。这是武器盒内的正常近战。
- 之后 F5 锁 `4661103` / `4662375` / `4661109` 等身边怪，`MapAtk oid0` 却是 `4713276`。锁目标 `hp` 不动、`hit=0` → `switch reason=whiff`。
- 23:03:32.232 关掉扩盒，23:03:32.542 `4661954 lastHitted`，`hp=100→52→37→27→15→死`。A/B：**关才打得动身边。**
- 第二窗锁 `4661110 hp=7`（关窗时打残的），再开扩盒后 `hit` 卡在 `161117`，`abandon_wounded_stall`。刀继续送给 1500px 外的蜗牛。

## 4. 判定

| 问题 | 答案 |
|---|---|
| 客户端 AABB 有没有写进 FindHit？ | 有。`exp=1`，尺度=本图 foothold。 |
| 远处怪进没进名单？ | 进了。而且 `mc=1` 时几乎**只**进池序第一只。 |
| 远处怪 hp 降了吗？ | **没有。** 72 刀 100%。 |
| 踢号了吗？ | **没有**（本段 kick 心跳干净）。 |
| 还能当「全图近战」卖吗？ | **不能。** 服端按范围砍。扩盒还会把近距那一刀偷给远处空包，现网打怪变差。 |

门禁原文：盒外怪不掉血 → 结案不可做；保持默认关；**禁止**改去造包。

抬 `maxCount` 把远+近塞进同一包：远的已被证不服，近的也许还认，但包里带一串越距 oid 是踢号高发区。本轮不试。

## 5. 样本（原文）

```
23:03:09.087 MapAtk exp=1 ... orig=1786,-342,80,40 oid0=4659183 dx=-64 dy=-13
23:03:09.100 combat whiff clear id=4659183 hp=100→100 hit=0→100457   // 近距真伤
23:03:09.974 MapAtk oid0=4713276 dx=-6 dy=542
23:03:10.079 combat whiff tick id=4661103 hp=100 hit=0               // 锁近、包远
23:03:14.652 mobscan [0] 4713276 hp=100% (1843,175)
23:03:32.232 MapAtk enabled=0
23:03:32.542 combat lastHitted id=4661954 hp=100→100 hit=0→123977   // 关开关恢复
23:04:12.887 MapAtk oid0=4713276 dx=1311 dy=542                     // 图左仍钉上层
```
