# 吸怪断连：平行检测族（拉怪 / 密度 / 死点 / HackLog）· Classic TWMS

> **产品**：新枫之谷：经典版（TWMS / `Maplestory_Classic.exe`）。**不是**枫星。
> **状态**：CMS dump 的 LiveValue **id 名仍实锤**；2026-08-22 晚把「检测函数」对回 `Dumps/runtime/out/dump.cs` 后，先前当 B/C/D/E 钩的 RVA **几乎全是 UI**（包裹 / 商城弹窗），不是 `Mob.InspectUpdateActive`。v1 五头计数=0 与此自洽。205 更宜按族 A 钉。
> **证据源**：`Dumps/cms_cw/dump.cs`（`LiveValueInt` / `LiveValueString`）· `Dumps/runtime/GameAssembly.dll.i64`（imagebase **`0x7ffd60830000`**）· BIN `x.jsonl`（r=1000 + leash 仍 205，见 [`怪速举报type21与被动插值.md`](怪速举报type21与被动插值.md) §7.12）
> **日期**：2026-08-22
> **目的**：纠正「一根 `MobHackLogDisconnectCount`(408) 会话计数器」过窄模型；列出吸怪会**同时**撞上的平行族，并标明每条的证据等级。
> **关联**：[`怪速举报type21与被动插值.md`](怪速举报type21与被动插值.md) · [`客户端Hack标志与服端推断.md`](客户端Hack标志与服端推断.md) · [`../mob_gather/模块设计.md`](../mob_gather/模块设计.md)

---

## 0. 一句话

吸怪断连（`pendingError=205` / `lean_local_or_soft`）不是单因。客户端至少有 **四条互不替代的怪侧通道**：

| 通道 | 典型 LiveValue | 客户端会不会自己发包 | 和吸怪的对法 |
|---|---|---|---|
| **A 移动包位移** | 无（服端自量） | 正常移动包带**新坐标** | **本轮 205 主嫌疑**；leash 夹的是下一拍目标，`disp` 是已发生的 AP 跳 |
| **B 拉怪 Kick** | **926 / 927 / 928** 枚举实锤 | 专用 thunk **有**人 call，但 call 点在**包裹 UI** | 吸怪战斗路径上 **v1 函数头 0 次** |
| **C 密度** | **932–934 · 938–941** 枚举实锤 | 同左，函数在 `UIParcelDialog` 方法表 | 同上 |
| **D 死点** | **935–937** 枚举实锤 | 同左 | 同上 |
| **E 408** | **408** 枚举名仍是 DisconnectCount | 曾当「枢纽」的 `61540600` 是 **`UICashShopPopup` 静态工厂** | 商城弹窗，不是 HackLog 累加器 |

408 **不是** 928 / 934 / 936 的别名。prevpos 字节补丁只动 **A**，BIN 已证伪对 205 无效。

### 0.1 归属更正（2026-08-22 18:55 · runtime `dump.cs` + cms_cw 明文）

先前用「谁 call 了 GetInt thunk 927 / 谁 `object_new`+`60F87610`」当检测函数。专用 thunk 的 **id 解算仍对**（927 thunk `61FB17D0`：`ecx=0x57937195 + dword_670EF694` → **927**）。但 **code xref 只有包裹 UI**：

| RVA / VA | runtime dump 名 | cms_cw 对照 |
|---|---|---|
| `0x1C19BA0` `62449BA0` | `OnButtonClicked(ButtonInfo)` | `UIParcelDialog`（`[UIPrefab("UIParcelDialog")]`）；`ButtonInfo.Close=2000 … Money=2020 … QuickTaxHide=2033` 与 hashed enum **逐项相同** |
| `0x1C1E8A0` `6244E8A0` | **`SetMoney()`** | `UIParcelDialogQuickSend.SetMoney` |
| `0x1C1C150` `6244C150` | **`SetMoney()`** | `UIParcelDialogSend.SetMoney` |
| `0x1C1D430` `6244D430` | 与上同一 class、紧挨 `SetMoney` | 寄件/快捷寄件（`SendQuickDelivery` 一类），**不是**拉怪 Kick |
| `0x1C1B5A0` `6244B5A0` | 与另一 `SetMoney` 同 class | 同上 |
| `0x1C1F730` / `0x1C275E0` / `0x1C13E00` / `0x1C14720` / `0x1C20360` | `UIParcelDialog` 及其 Receive/Send 子页 | `OnPointerDown` / `OnScroll` / `Awake` 邻居 |
| `0xD10600` `61540600` | `UICashShopPopup` 静态方法 `(string,string,bool,Action,bool)` | **商城弹窗工厂**，不是 408 HackLog 枢纽 |
| `0xBF5BB0` `61425BB0` | hashed UI 方法，**尾跳** `624575E0` | 仍是 UI 链 |

`60F87610` 是通用发送 helper（~100 调用方）：包裹寄信也会 `object_new`+send，**不能**单凭它当 Kick。

**活的怪侧 tick**仍是 `VecCtrlMob.InspectUpdateActive` RVA **`0x11DBAB0`**（`mob_fh_ban.cpp` `kRvaInspect` 已 MethodInfo 钩，不要再 abs-jmp 撞车）。`SendMobPrevPosHack` 内联在 `0xF59D50`，无独立入口。

---

## 1. 证据边界

| 等级 | 含义 |
|---|---|
| **实锤** | dump 符号 + 当前 IDB 解出 id 与 thunk 一致（种子实读）+ 代码 xref |
| **强推断** | 命名 + call 序 + BIN 时空对得上，比较式未完全去 CFF |
| **未钉** | 运行时现货、单位、本轮 18 次 205 的具体族、服端是否再算一遍 |

**禁止**：把旧笔记 RVA 当成本 IDB 地址。`0x1774C90` 曾被当成 926，当前 dump 解出来是 **856**（FastAccess）。GetInt 公共 helper 是 `sub_7FFD61F08680`（RVA `0x16D8680`，约 5634 调用方）：`mov ecx, IMM` → `xor`/`add ecx, cs:seed` → `call helper`。

CFF 函数的 Hex-Rays 伪代码几乎只有跳表初始化，**以 `insn_query` / 实读种子为准**。`jmp cs:off_*` 的 IDA 静态目标经常指到 epilogue，不可当成「GetInt 之后立刻 return」。

---

## 2. DUMP 键表（`dump.cs`）

### 2.1 `LiveValueInt`（怪侧、与吸怪相关）

| id | 名 | 角色 |
|---|---|---|
| **926** | `MobPullingHack` | 拉怪检测 On |
| **927** | `MobPullingHackThreshold` | 拉怪阈值 |
| **928** | `MobPullingHackKick` | **拉怪踢** |
| 929–931 | `SpeedAnalyzer` / `Threshold` / `Kick` | 移速分析；与拉怪检测函数**同文件簇** |
| **932** | `MobDensityByCentroid` | 质心密度 On |
| **933** | `MobDensityByCentroidCount` | 只数 |
| **934** | `MobDensityByCentroidThreshold` | 质心密度阈值 |
| **935** | `MobDeadPositionInspect` | 死亡点 On |
| **936** | `MobDeadPositionInspectThreshold` | 死亡点阈值 |
| **937** | `MobDeadPositionInspectLogCount` | 死亡点日志次数（名是 Log 不是 Kick） |
| **938** | `MobDensityBySector` | 扇区密度 On |
| **939** | `MobDensityBySectorCount` | 扇区只数 |
| **940** | `MobDensityBySectorSize` | 格子尺寸 |
| **941** | `MobDensityBySectorThresholdPct` | 扇区占比阈 |
| **408** | `MobHackLogDisconnectCount` | HackLog → 断线阈值（表 id，不是进程里的累加器变量） |
| 411 | `FindMobInRectHackLogDisconnectCount` | 矩形找怪 |
| 412 | `MobPauseServerCheck` | 暂停/挂起 |
| 415 | `TargetMobInspect_InflateRectVal` | 抽检矩形膨胀 |
| 335 / 354 | `MobCrcCheck` / `_SelfCalc` | 怪动作 CRC |
| 525 | `MobNotMoveHackCheckValue` | 该动不动 |
| 710 / 718 | `CheckMobActionCount` OnOff / Weight | 动作次数 |
| 781 | `CheckMobCountInFewKeyCount_Threshold` | 按键少、怪死得多 |
| 220 | `PreventPrivateServerOnOff` | 拉怪 Kick 发送前也读（可能是跳过私服/反作弊门） |

### 2.2 `LiveValueString` 地图白名单（SkipField）

| id | 名 | 含义 |
|---|---|---|
| 102 / 103 | `SpeedAnalyzerSkipMPA` / `SkipField` | 移速分析可跳图 |
| **104** | `MobDeadPositionInspectSkipField` | 死亡点可跳图 |
| **105** | `MobDensityByCentroidSkipField` | 质心密度可跳图 |
| **106** | `MobDensityBySectorSkipField` | 扇区密度可跳图 |

**dump 里没有 `MobPullingHackSkipField`。** 拉怪族不能靠字符串 LV 按图关掉——和密度/死点不对称。

---

## 3. IDA：GetInt thunk（当前 IDB · 种子已验）

imagebase `0x7ffd60830000`。thunk 全部 `call sub_7FFD61F08680`。

| LV | thunk VA | RVA | 混淆 | 解出 |
|---|---|---|---|---|
| 335 | `0x7ffd61f45c50` | `0x1715C50` | add | 335 |
| **408** | `0x7ffd61f52bf0` | `0x1722BF0` | add | **408** |
| 411 | `0x7ffd61f53480` | `0x1723480` | add | 411 |
| 412 | `0x7FFD61f53750` | `0x1723750` | add | 412 |
| 525 | `0x7ffd61f687e0` | `0x17387E0` | add | 525 |
| 710 | `0x7ffd61f89a20` | `0x1759A20` | xor | 710 |
| 781 | `0x7ffd61f969e0` | `0x17669E0` | add | 781 |
| **926** | `0x7ffd61fb1490` | `0x1781490` | add | **926** |
| **927** | `0x7ffd61fb17d0` | `0x17817D0` | add | **927** |
| **928** | `0x7ffd61fb1a60` | `0x1781A60` | add | **928** |
| 929 | `0x7ffd61fb1d50` | `0x1781D50` | xor | 929 |
| 930 | `0x7ffd61fb2050` | `0x1782050` | xor | 930 |
| **932** | `0x7ffd61fb25e0` | `0x17825E0` | xor | **932** |
| **933** | `0x7ffd61fb28a0` | `0x17828A0` | add | **933** |
| **934** | `0x7ffd61fb2c30` | `0x1782C30` | add | **934** |
| **935** | `0x7ffd61fb2fb0` | `0x1782FB0` | xor | **935** |
| **936** | `0x7ffd61fb3220` | `0x1783220` | add | **936** |
| **937** | `0x7ffd61fb34d0` | `0x17834D0` | xor | **937** |
| **938** | `0x7ffd61fb37f0` | `0x17837F0` | xor | **938** |
| **939** | `0x7ffd61fb3ae0` | `0x1783AE0` | add | **939** |
| **940** | `0x7ffd61fb3e10` | `0x1783E10` | xor | **940** |
| **941** | `0x7ffd61fb4120` | `0x1784120` | add | **941** |
| 220 | `0x7ffd61f30bd0` | `0x1700BD0` | add | 220 |

取值链路（拉怪 Kick 段，重复三次：927 / 928 / 220）：

```text
GetInt(id) thunk
  → sub_7FFD6135FDF0     ; 包装/解盒
  → sub_7FFD61F1FE90
  → sub_7FFD60F8B6A0     ; 公共「取出 int」（调用方极多）
```

---

## 4. 族 B · 拉怪 Kick（枚举实锤；**客户端 call 点是包裹 UI**）

### 4.1 函数

| 函数 | VA | RVA | dump 真名 | 作用（更正后） |
|---|---|---|---|---|
| 「分发器」 | `62449BA0` | `0x1C19BA0` | **`UIParcelDialog.OnButtonClicked`** | 点包裹按钮；14 case ≈ ButtonInfo 2000–2033 |
| 「On 包装」 | `6244C150` · `6244E8A0` | `0x1C1C150` / `0x1C1E8A0` | **`SetMoney()`** | 刷新寄件金额 UI |
| 「阈值+Kick」 | `6244D430` | `0x1C1D430` | 与 `SetMoney` 同 class | 寄件/快捷寄件；体内 **确有** `call GetInt_927`（种子实算 927），但是 **UI 路径** |
| 「另一 Kick」 | `6244B5A0` | `0x1C1B5A0` | 另一 `SetMoney` 邻居 | 同上 |
| 「死亡点包装」 | `62443E00` | `0x1C13E00` | `UIParcelDialog` 方法 | UI，不是死点 Inspect |

专用 thunk `61FB17D0` 在本 IDB **只有这一处 code xref**（`6244E440`）。战斗吸怪不会进这些函数 → v1 `pullKick=0` 是预期，不是「CFF 跳过序言」。

`60F87610` 仍可能从寄件 UI 发包（包裹协议），**不要**写成 928 Kick。

### 4.2 已还原的算术（`6244D430` · 种子实读）

函数内大量 `cmp eax, edx` 解出来是 **0**，是 il2cpp `klass[+0xE4]` 初始化门，**不是阈值**。

寄件函数里的算术（不要当拉怪阈值）：

```text
0x6244E592  rdx = 0xAC49920354A5ADDA + [0x7FFD67114F18]
            → rdx = 100000000 (0x5F5E100)     ; 实锤
            cmp rax, rdx / jge …
0x6244E5BE  cvtsi2sd xmm0, rax
0x6244E5C9  mulsd xmm0, [0x7FFD66104F98]
            → 乘数 = 0.06                     ; 实锤（IEEE double）
0x6244E5D7  cvttsd2si rcx, xmm0
```

**强推断（单位未钉）**：`0.06 ≈ 1/16.67`，像「毫秒 × 0.06 ≈ 60fps 帧数」；`100000000` 像 .NET `TimeSpan` 10 秒的 tick（10×10⁷）或溢出哨兵。不能写成「阈值 = 100000000 px」。

随后还有 `GetInt` / `object_new` / `60F87610`。在「这是寄件 UI」前提下，**0.06 与 1e8 更像金币/税率/TimeSpan，不要再当拉怪 px 阈值**。GetInt(927) 出现在寄件函数里仍然奇怪（CFF 邻接或寄件也读反作弊表）——**战斗路径未使用**已由 v1 BIN 钉死。

### 4.3 与 BIN

v1 吸怪 205 全程 `pullKick=0`：这些函数根本没进。K10 快死更像族 A 一次大位移，不是 928 Kick。

---

## 5. 族 C · 密度（枚举实锤；函数在包裹 UI 方法表）

`62450360`（RVA `0x1C20360`）在 runtime dump 里是 `UIParcelDialog` 子页方法（`OnScroll` / `OnPointerDown` 邻居）。体内仍可能 `call` 932–934 thunk——只说明**有 UI 代码读了这些键**，不说明吸怪时会跑。v1 `density=0` 符合「没开包裹」。

## 6. 族 D · 死亡点（枚举实锤；函数在包裹 UI 方法表）

`62444720`（RVA `0x1C14720`）同上。SkipField 104/106 作为**表项**仍在 dump 里；本局 205 不能归因到这条客户端函数。

---

## 7. 族 E · 408（枚举实锤；`61540600` **不是**枢纽）

- thunk `61F52BF0` = **408**（不是旧 RVA `0x1715E70`；那条是 **335 CRC**）。
- `sub_7FFD61540600`（RVA `0xD10600`）在 runtime dump 里是 **`UICashShopPopup` 静态工厂**（`[UIPrefab("UICashShopPopup")]`），签名 `(string, string, bool, Action, bool)`。xref 成片 = 很多 UI 要弹商城窗，**不是**「各 Inspect 送 HackLog」。
- 408 仍是表 id `MobHackLogDisconnectCount`；真正谁在战斗里 GetInt(408) **未钉**（可能走泛型 `LiveValue.GetInt` 而不是专用 thunk）。

压 408 / 压 prevpos 都挡不住服端自量位移（族 A）。

---

## 8. 其它同向族（证据弱于 B–D，但 dump 有名）

| 键 | thunk / 调用 | 吸怪怎么撞 | 等级 |
|---|---|---|---|
| 781 少键多怪 | thunk `61F969E0`；`sub_7FFD616BD230` 读 781 并解包；**`sub_7FFD62388B10` 读 781 且 `object_new`+`60F87610` send** | 堆好后攻速清一圈 | 发送路径实锤，阈值公式未钉。同函数还 call 了 782–786 thunk（物价预测器 / unused），那是 CFF 邻接，**不是**少键公式的一部分 |
| 710 / 718 动作计数 | `61F89A20` → `61D13920` | Impact 改动作，官方计数对不上 | 符号实锤 |
| 335 CRC | `61F45C50` | 硬改 Ap / 跳过官方 action | 符号实锤 |
| 525 该动不动 | `61F687E0` → 大函数 `61555E60` | 悬停锁台 | 符号实锤 |
| 412 Pause | `61F53750` | 武装期停 AI | 符号实锤 |
| 411 矩形找怪 | `61F53480` | 旧笔记 imagebase `0x7FF848C80000` **作废** | 仅符号 |

`SendMobPrevPosHack` 仍内联在 `sub_7FFD61789D50`（RVA `0xF59D50`），与上述 Inspect **不是同一个函数**。见 type21 文 §7.11–§7.12。

---

## 9. 和「近怪第二波仍断」怎么叠（更正后）

1. **位移包（A）**：服端自量新坐标。leash 夹下一拍目标；`MobFhBan impact … disp=250–430 big=1` 是**已经发生的** AP 跳（相对上次记录），可以大于 `cap=48`。hop 默认 950px。策略 B `fh-snap` 一帧写落点、不 leash。
2. **补刷**：`gSpawnGateOn` 软重连不复位 → 从 spawn 再拽几百 px，放大族 A。
3. **B/C/D 客户端函数**：包裹 UI，吸怪战斗不进。
4. **408 / 商城弹窗**：不是本局 205 开关。
5. **活 tick**：`InspectUpdateActive` `0x11DBAB0`（gather 已钩 MethodInfo）。

`ours=0` 与族 A 正交：没控制权照样改本地 Ap，移动包仍带新坐标。

---

## 10. 还没钉（下一轮）

| 项 | 怎么钉 | 现状 |
|---|---|---|
| 926–941 / 408 **现货** | 泵上直调专用 thunk（v2） | v2 待再注。FindClass(`LiveValue`) 会 miss（TW 哈希名） |
| 战斗里谁 GetInt(926–941) | 泛型 `GetInt(id)` / `InspectUpdateActive` 去 CFF | 专用 927 thunk **仅**包裹 UI 一处 code xref |
| 族 A 单帧 `disp>cap` | 对齐 `impact disp=` 与 205；hop/首帧/策略 B | BIN 已见 250–430 / `big=1`；**不要**当 B/C/D 客户端 Kick |
| 781 公式 | `62388B10` 等是否也是 UI | 未按 UIPrefab 复核 |
| 服端密度/死点 | 无服端二进制 | 无法用客户端证伪 |

探针约束：这些路径有 `il2cpp_runtime_class_init` / `object_new`，**只许 MainPump 上读**；不要在 worker 里 hook 直调。

### 10.1 BIN 探针（2026-08-22，**默认关**）

模块 `x/features/ports/mob_inspect_probe.{h,cpp}`。默认不挂钩、不脏 GA .text。开：进程环境变量 **`MOB_INSPECT_PROBE=1`**。**v2 仍钩这些 UI RVA**（负对照：吸怪不应响；**开包裹/点寄件**应见 `edge dispatch` / `pullKick`）。开启后进图：

1. **abs-jmp** 五个函数头（现知是 UI，负对照）：
   - `0x1C1D430` `pullKick`（`SetMoney` 邻居，体内 call GetInt_927）
   - `0x1C1B5A0` `pullSend`
   - `0x1C20360` `density`（包裹子页）
   - `0x1C14720` `deadPos`
   - `0xD10600` `hub408`（**`UICashShopPopup` 工厂**）
2. **v2 Rel5** 父函数（同样是 UI 链）：
   - `0xBF5BB0` `sched`（尾跳 `tick`）
   - `0x1C19BA0` `dispatch` = **`UIParcelDialog.OnButtonClicked`**
   - `0x1C275E0` `tick`
   - `0x1C1F730` `densWrap`
   - `0x1C13E00` `deadWrap`
3. 钩子里**只计数 + 250ms 边沿日志**，不调 GetInt。
4. 主泵每 15s **直调 §3 专用 GetInt thunk**（`ecx=default=-999999`，不 `FindClass`）。返回值像指针再走 `6135FDF0` 解盒。缺席/异常=`-999999`。

`x.jsonl` tag=`MobInspect`。对齐 KickSniff 205：看断前 1–2s 的 `edge sched|dispatch|tick|densWrap|deadWrap|pullKick|…`。默认关；开：`MOB_INSPECT_PROBE=1`。卸载还原 .text。Rel5/abs-jmp 都脏 GA .text（与 melee_veto 同类，可能碰 MemoryCrc）。

**读日志**：

| `hb` | 含义 |
|---|---|
| 吸怪时全部 0 | **预期**（这些是包裹/商城 UI） |
| 开包裹后 `dispatch`/`pullKick`>0 | 负对照成立：钩在对的 UI 上 |
| `lv 926=…` | LiveValue 现货（与 UI 是否打开无关） |

**BIN v1（18:35 注入 + 18:39 再注，`built=18:34:38`）**：

- 安装实锤：`armed` + `hooks on n=5`（RVA 与 IDA 一致）。
- 真踢（无 `Nm.CloseSession` / `proactive=0`）：18:36:31、18:36:40、18:40:23、18:40:44、18:41:05。断前都有 `MobGather apply` + `impact … disp≈250–430 big=1`。
- 全程 `hb pullKick=0 pullSend=0 density=0 deadPos=0 hub408=0`，**零条 `edge`**。
- `LiveValue klass miss`（TW 类名哈希，`FindClass("Msc.Common","LiveValue")` 落空）——v1 没采到现货，不影响「检测函数头没进」。
- 强推断：这次 205 **不是**这些函数发出的。事后对 dump：**它们是包裹/商城 UI**，吸怪战斗本来就不会进。v2 仍钩它们做负对照，并泵上直调 GetInt thunk 采现货。**请再注 `built=18:58:29` 的 `xcat.dll`。**

---

## 11. 对吸怪实现的含义（不本轮改 gather 夹速）

- **不要**再指望 prevpos / 充 `e` / 翻 `cmovz`。
- **不要**再把 `6244D430` 当拉怪 Kick 去堵；那是包裹 UI。
- 降 205 仍只能让服端量不到大位移：策略 A + 真把逐帧 AP 跳压到门限内（注意 `disp` 是已发生量，hop 950 / 策略 B 瞬移都会破）、软重连复位 spawn 闸、人或落点去怪。
- `InspectUpdateActive` 已在 gather 里钩，不要再 abs-jmp。
