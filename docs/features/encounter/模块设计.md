# encounter Feature 模块设计（Classic TWMS）

> **状态**：✅ 已挂入 `xcat.dll`（契约 v20 + UI + UserPool 采样 + FSM；冷却/pause 与 channel_hop 对齐）  
> **源码根**：`x/features/encounter/` · 探测：`x/features/ports/user_pool_port.*` · 换频执行：`channel_hop`  
> **对照**：枫星 `auto_relogin` / `encounter_strategy`（**SceneManager.Reload + UserService，不可复用**）  
> **产品**：经典版 TWMS · **不是**枫星  
> **安全**：遵守 [`../security/GRAP与枫星对齐.md`](../security/GRAP与枫星对齐.md) — **禁止 E9 / 改 `GameAssembly.dll` `.text`**。

## 职责

- 「遇人策略」Tab 四勾选 → `[core] autoRelogin*` 落盘
- 检测同图 **远程玩家**（`UserPool` 字典人数，**禁止** CCU / 选角 AvatarCount）
- 按策略：先停手 / 持续有人则 `channel_hop::RequestManualRejoin`
- **不开**换频菜单；**不** `SceneManager.Reload`；**不**建命名管道

## 与枫星差异

| | 枫星 | TWMS Classic |
|---|---|---|
| 检测 | `UserService.mapToUserEntities` | `UserPool` Singleton + `Dictionary@+0x20` count |
| 换频 | `SceneManager.Reload(false)` | `channel_hop` → `SendTransferChannelRequest` |
| 停手 | named external pause | `simple_combat::SetExternalPause`（测谎 busy 时不抢清） |

## 控制面

| 项 | 值 |
|---|---|
| INI | `autoRelogin` / `autoReloginStopCombat` / `autoReloginReconnect` |
| core version | `kPayloadControlCoreIniVersion = 20` |
| 默认 | 主开关关；子策略停手/停飞/换频默认开（对齐枫星） |

## 逆向锚点

| 项 | TW 实锤 |
|---|---|
| 类 | hashed `e0e036a9…` TypeDef 1582（CMS `UserPool`） |
| `get_UserLocal` | RVA **`0x110E230`** → `*(this+0x10)` |
| `GetRemoteUserCount` | RVA **`0x110E240`**（CFA；采样优先读字段） |
| 远程字典 | `+0x20` → IL2CPP `Dictionary.count@+0x20` |
| 列表兜底 | `+0x18` → `List._size@+0x18` |

## FSM

| 状态 | 行为 |
|---|---|
| Idle | 主开关关或无人 |
| Watching | 已 pause；未勾换频 / 进图 grace / **channel_hop 忙或冷却** / 主城跳过 |
| Confirming | 连续 ≥4s 有人且可 hop |
| Hopping | 已请求换频；等 channel_hop 结束 + **≥22s 宽限**（并与 `CooldownRemainingMs` 取大） |

常量：`kConfirmMs=4000` · `kCheckIntervalMs=1000` · `kFirstLandGraceMs=5000` · `kPostHopGraceMs=22000`。

门控：`IsPlayReady` · 非商城/登录 · `!auto_lie::IsBusy()` · 主城启发式（图号中间三位 000）· **`!channel_hop::HasPending` · `CooldownRemainingMs==0`**。

### 与 channel_hop 协作（BIN 2026-08-02）

| 问题 | 修法 |
|---|---|
| 8s 宽限 << 成功冷却 → 连烧 | `kPostHopGraceMs=22s` + Hopping 内对齐 `CooldownRemainingMs` |
| 冷却中仍 RequestHop → defer 风暴 | RequestHop / Confirm 前检查冷却；只 Watching，不烧 seq |
| Fail/OK 后 resume 清掉遇人 pause | `HoldsCombatPause()`；`ResumeCombatAfterHop` 跳过抢清 |

## 验收

1. 勾「检测」：同图他人 → 停手/停飞 + 通知；人走净 → 恢复（曾飞则回飞）  
2. 勾「一直有人就换频」：≥4s → 无菜单换频（`Encounter` + `ChannelHop` 日志）  
3. 测谎中不 hop；Resume 不抢测谎 pause  
4. 主开关关无动作  
5. 成功换频后 20s 内不应再 `hop seq=`（可有 `hop defer: cooldown`）  
6. 警戒超时 Fail 后，遇人 pause 应保持（日志 `combat resume skipped`），不应立刻连烧下一 seq  

## 非目标

- 不移植枫星 Reload / UGC / msw 守护  
- 不用频道 CCU 冒充同图人数  
- 「随机换频」按钮仍属 `channel_hop` / `manualRejoinSeq`
