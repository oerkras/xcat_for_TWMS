# --- Build / CMake outputs ---
/bin/
/bin_ops/
/bin_ops_staging/
/build/
/build_xcat_for_TWMS/
**/CMakeCache.txt
**/CMakeFiles/
**/cmake_install.cmake
**/Makefile
**/compile_commands.json
**/*.vcxproj
**/*.vcxproj.filters
**/*.sln
!**/CMakeLists.txt
# Kernel research tool (WDK msbuild); keep checked in.
!/tools/krw/**/*.vcxproj
!/tools/krw/**/*.vcxproj.filters
!/tools/krw/**/*.sln
!/tools/krw/**/*.inf

# Nested build trees (keep NuGet package trees under third_party)
/launcher/build/
/xcat_app/build/
/ops_app/build/
/injector/build/
/common/build/
/sound_cli/build/

# --- RE dumps & restored dumps (multi-GB) ---
/Dumps/
/DumpRestoredData/
/OLD DUMP DATA/

# Local driver/signing toolkits (not product source)
/DriverTools/

# --- Release packages & local runtime ---
/artifacts/
/user_log_uploads/
/publish_site/downloads/
/publish_site/update/*.zip
# Stray release zips if publish ever copies into repo root (cwd) instead of publish_site/downloads
/xcat_for_twms_*.zip
/tools/bin/
**/__pycache__/
*.pyc

# Runtime data under output dirs
**/webview_profile/
**/XCat_data/
**/XCat_datastate/
**/xcat_imgui.ini

# tools/krw intermediates
/tools/krw/**/obj/
/tools/krw/**/obj_*/
/bin/krw/

# Local backups / scratch
/ops_app/_replay_backup/

# --- Compilers / MSVC intermediates ---
*.obj
*.o
*.pdb
*.ilk
*.exp
*.idb
*.ipdb
*.iobj
*.sbr
*.bsc
*.pch
*.gch
*.ncb
*.aps
*.tmp
*.temp

# --- Logs ---
*.log
*.jsonl

# --- IDE / OS ---
.vs/
.idea/
*.user
*.suo
*.sdf
*.opensdf
*.VC.db
*.VC.opendb
Thumbs.db
Desktop.ini
.DS_Store

# --- Secrets / local env ---
.env
.env.*
!.env.example
*.pem
*.pfx
*.p12
credentials.json
xcat_app/anti_bot_ai/llm_secrets.h
# gate/1 授权敏感数据：签发台账（含成员 uid 清单）与私钥，禁止入库/外发。
# （台账已被上面的 /artifacts/ 覆盖，这里显式再兜一层并标注意图。）
artifacts/ops_logs/gate_cards.jsonl
secrets/gate_ec_priv.pem
# 运维台账：成员 uid / IP / 机名 / 角色名等可定位信息，同样禁止入库。
artifacts/release/client-history.json
artifacts/release/device-access.json
artifacts/release/device-quota.json
artifacts/release/update-channels.json

# 卷轴语音烘培中间 mp3（入库只留 wav + tsv）
/dumps/offline_tables/scroll_voice/*.mp3

bin_ops_staging/
